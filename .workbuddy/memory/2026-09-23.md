# 2026-09-23 工作日志

## 无语子点点公众号同步（自动化 d41bb672）
- 08:00 首次跑：微信在运行但 harvest 失败（找不到内置浏览器窗口）→ 0 新文章，commit `1dabaac` 已 push。
- 09:18-09:35 深度排查并修复 `tools/harvest_list.py`（+225/-41 行），commit `b60fc6d` 已 push。
- **连续两天失败的根因**（三条叠加）：
  1. 微信 4.0 内置浏览器是**主窗口的子窗口**（渲染进程 `WeChatAppEx.exe`），旧 `find()` 只枚举顶层窗口 → 永远看不见；
  2. 微信**最小化到托盘时 WebView 不渲染**，UIA 里没有 DocumentControl → 必须先 `SW_RESTORE` 唤醒
     （`SW_SHOWNOACTIVATE` 对最小化窗口无效），结束后还原最小化；
  3. 唤醒后 WebView 需数秒重建渲染 → 轮询等待；并按 `WeChatAppEx.exe` 过滤，避免误抓 Wind(`wmain.exe`) 的微信文章窗口。
- 点击策略改为 real(真实鼠标)→host(直投 Chrome_RenderWidgetHostHWND)→point 三策略 + URL 校验。
- 验证：最小化状态下能唤醒→命中 WebView→读 profile URL→`articles()` 解析出列表（10 项，行距 331px）。
  点击打开文章 / 名片回主页 尚未验证（测试中途该号 WebView 面板被关闭）。
- 另一条重要结论：**列表发现无法脱离微信**——实测封死 8 条免会话路径（无 key/无 chksm/移动 UA/getmsg/合集/搜狗/mid 探测/普通浏览器）。
  只有单篇正文可以裸 HTTP 直取（chksm 必需，且是硬门槛）。

## 残留依赖
- 微信里必须存在该号 WebView（主页或任意文章页）。若丢失需人工打开一次；彻底无人值守需另加 OCR 视觉驱动开窗。
