# 2026-09-22 工作日志

## 无语子点点 公众号归档 · 增量同步（自动化 22:00）
- 微信 Weixin.exe 在运行，但**未打开过该号文章** → harvest_list.py 找不到微信内置浏览器窗口，抓取环节直接退出。
- 重试 1 次结果相同（持续性条件，非偶发）。
- 后端环节正常：fetch 76/76 通过、build 76 篇、git commit `a420798` 已 push 到 origin/main。
- 本次新增文章 **0 篇**；本地工作区干净，远端与本地 HEAD 一致。
- 待办性质结论：下次运行前需先在微信里点开「无语子点点」任意一篇文章，使内置浏览器窗口存在。

## 经验补充
- `sync.py` 的 harvest 失败不会返回非 0 退出码，也不会中止流程；只能通过 `harvest.log` 末行判断。
- 「微信进程在运行」不等于「可抓取」，前置条件分离判断。
