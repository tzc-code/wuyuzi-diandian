# 2026-09-24 工作日志

## 无语子点点 公众号文章增量同步（automation d41bb672）
- 08:00 触发。Weixin.exe 在运行（PID 31284），未跳过。
- harvest 全链路正常：命中 WebView → 已在公众号主页 → 增量 0 → 点首条命中已知 mid=2247484330 → 结束（11s）。
- fetch 76/76 OK（92s）；build 76 篇；**新增 0 篇**。
- commit `9b28b7d` 已 push，`HEAD == origin/main`。变更仅 README 同步时间戳 + memory 文件。
- 最新文章仍是 2026-09-18「石油和粮食」（ts=1776740412）。

## 无语子点点 增量同步 · 手工排障（13:36-14:00，大王告知有新文）
- 上午 08:00 判定「0 新增」是**正确的**；新文是随后（今日 12:59）才发布的。
- 新文：**`。。` — 2026-09-24 12:59**，副标题「房地产回暖不等于公司盈利，消费医药或成新热点」，mid=`2247484333`。
- 三次 harvest 失败链路（都在 `tools/harvest_list.py` 的实现在场条件下发生）：
  1. `find()` 命中 2026-09-23 打开的**陈旧 profile 页**（72 篇）→ 列表无新文 + 点击 MISS。
  2. 命中新页面后，`_rebind()` 命中**仍开着的旧文章页** → 取回旧 mid → 误判「已知」提前结束。
  3. 新文章页一直开着 → 同一篇被**重复收集**（collected 78）。
- 环境差异：`real` 合成鼠标点击在全屏 WorkBuddy 窗口遮挡下**静默失效**；
  `host`（PostMessage 直投渲染窗口）一次成功。
- 收尾：`articles.json` 由 78 去重回 77（备份恢复 + 单条正确记录），重跑 `build_repo.py`；
  commit `b2681aa` + `3eccfe6`，`HEAD == origin/main`，仓库 77 篇。
- 教训已写入 skill `wechat-mp-archive-github`（新增第 9 节：三种隐蔽失败模式 + 别用 `| head` 截 sync 输出）。

## 无语子点点 增量同步（automation 53df7580，22:00 夜间跑批）
- 22:00 触发。Weixin.exe 在运行（PID 31284），未跳过。
- **harvest 环节失败**：`扫描 25s 未发现 WeChatAppEx 渲染的页面` + `登录态判定未登录(或仍在登录窗口)` → `!! 找不到该号的 WebView`。
  - 注意 WeChatAppEx.exe 进程**存在**（3 个），说明不是「没有渲染进程」，而是没有承载 mp.weixin.qq.com 的可抓页面 / 主窗口 UIA 看不到导航栏。
- 重试 1 次（共 2 次，达规程上限），结果完全一致 → 持续性环境条件，非偶发。
- fetch/build 沿用既有 77 篇（本 run 未有新文入库）；**本次新增 0 篇**。
- 09-24 那篇 `。。` 是 13:55 那轮（commit `b2681aa`）已入库的，非本轮新增。
- git：`4eb4948`、`a62be21` 两次 README 时间戳提交均已 push，`HEAD == origin/main == a62be21`。
- 恢复条件：需人工在微信里打开「无语子点点」任意一篇文章（或确认已登录非二维码界面），下次跑批即可自动恢复。

