# 2026-09-26 工作日志

## 08:00 自动任务：无语子点点公众号归档同步（失败：微信掉登录）

- Weixin.exe 在运行（PID 31284）→ 未走「微信未启动」跳过分支。
- `tools/sync.py` 跑完 rc=0，但 **harvest 全废**：
  - `扫描 25s 未发现 WeChatAppEx 渲染的页面`
  - `微信登录态: 未登录(或仍在登录窗口) -> 需要人工扫码`
- 独立诊断（直读主窗口 UIA）确认根因：主窗口 hwnd=13372594、title=`微信`、
  rect=`(-1108, 325, -812, 713)`（380x388 在屏外）、`is_logged_in()=False`；
  UI 文本 = `提示` / `你已退出微信` / `我知道了` / `当前登录用户长风⁶ᯤ` / `进入微信` / `切换账号` / `仅传输文件`。
  ⇒ **微信已退出登录，须人工扫码**（与 2026-09-25 完全同一模式，skill 第 9 节「模式 D」）。
- 「进程在 ≠ 可抓取」再次验证：WeChatAppEx 无页面，全桌面 WebView doc 数 = 0。
- 未反复重试：根因是需人工扫码，重跑无意义（09-25 已实测重试结论一致）。
- fetch 77/77 OK（全旧文）→ build 77 → commit `afd9c2f`（2 files：README 仅同步时间戳 +
  另一 automation 的 memory.md）已 push，`HEAD == origin/main == afd9c2f`。
  该 commit 属**失败副产物**，非「0 新增正常」。
- 本次新增 0 篇。

### 下次跑之前的前置检查（重要，延续 09-25 教训）
1. Weixin.exe 进程在不在（必要非充分）；
2. **主窗口 UIA 是否含「你已退出微信」** → 有则必失败，需人工扫码；
3. **该号 WebView 面板是否存在**（全桌面扫 DocumentControl 看是否含 `SubscriptionProfile/profile.html`）。
