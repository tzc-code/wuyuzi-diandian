# 2026-09-25 工作日志

## 08:00 定时增量同步（无语子点点 → GitHub）— **失败：微信掉登录**
- Weixin.exe 在运行（PID 31284）→ 未触发「微信未启动则跳过」。
- `tools/sync.py` rc=0，但 **harvest 抓取全废**：日志 `扫描 25s 未发现 WeChatAppEx 渲染的页面`
  + `微信登录态: **未登录(或仍在登录窗口)**`。0 新增（不是真的没新文，是压根没抓到列表）。
- fetch 77/77 全 OK（全是库里旧文）→ build 77 篇 → commit `ed766ab`（README 时间戳 + 2 memory）已 push，
  `HEAD == origin/main == ed766ab`。
- **根因确诊**：读微信主窗口 UIA 得到 `TextControl '提示'` + `TextControl '你已退出微信'`
  + `ButtonControl '我知道了'/'切换账号'/'仅传输文件'` ⇒ **微信已退出登录，需人工扫码**。
  现场特征：主窗口异常小 `280x380` 且落在屏幕外（rect x 为负）；登录态正常约 `1079x883` 且屏幕内。
- 重试：sync 内 1 次 + 单跑 `harvest_list.py` 1 次，共 2 次，同一结论 → 停止重试（符合上限）。
- 待大王处理：① 微信扫码登录；② 登录后手动打开一次「无语子点点」主页/文章页（脚本无法自己开页）。
- 已把该失败模式写进 skill `wechat-mp-archive-github` 第 9 节「模式 D」。
- 注：`Weixin.exe` 在运行 ≠ 可抓取，前端 checklist 只查进程是不够的。

## 诊断手法备忘（本次新用，可复用）
- `Add-Type`（PowerShell 内联 C#）被安全策略拦截 → 截图路线走不通。
- **改用脚本环境自带 pywin32**：`win32gui.EnumWindows` 枚举窗口 + `win32process.GetWindowThreadProcessId`
  判断归属进程；再用 `uiautomation` 读控件树。比截图更精确，且无落盘残留。
- 本机解释器无 `psutil`；取进程 exe 名要用 `win32process.GetModuleFileNameEx`
  （`win32api.OpenProcess(0x1000)` 或 `0x0410`）。Bash heredoc 里反斜杠会被处理，路径切分别写 `'\\'`，用 `os.path.basename`。
